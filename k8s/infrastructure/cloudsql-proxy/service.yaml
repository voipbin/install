apiVersion: v1
kind: Service
metadata:
  name: cloudsql-proxy
  namespace: infrastructure
  labels:
    app: cloudsql-proxy
spec:
  type: ClusterIP
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
  selector:
    app: cloudsql-proxy
