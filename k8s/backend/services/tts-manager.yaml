apiVersion: apps/v1
kind: Deployment
metadata:
  name: tts-manager
  namespace: bin-manager
  labels:
    app: tts-manager
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tts-manager
  template:
    metadata:
      labels:
        app: tts-manager
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/path: "/metrics"
        prometheus.io/port: "2112"
    spec:
      containers:
        - name: tts-manager
          image: voipbin/tts-manager
          ports:
            - containerPort: 2112
              name: metrics
          envFrom:
            - configMapRef:
                name: voipbin-config
            - secretRef:
                name: voipbin-secret
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 2112
            initialDelaySeconds: 15
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 2112
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: tts-manager
  namespace: bin-manager
  labels:
    app: tts-manager
spec:
  type: ClusterIP
  ports:
    - name: metrics
      port: 2112
      targetPort: 2112
  selector:
    app: tts-manager
